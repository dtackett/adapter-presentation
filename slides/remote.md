##Here is our cool Remote

```
Class CoolRemote {
  var tv;

  SuperRemote(tv) {
    this.tv = tv;
  }

  function powerOn() { tv.powerOn() };
  function powerOff() { tv.powerOff() };
  function changeChannel(chan) { tv.changeChannel(chan) };
}
```
